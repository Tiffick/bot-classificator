===============================================================================
FIRST COGNITIVE CORE — TECHNICAL SLICE COMPLETE
===============================================================================

Commit:
2f38b47 Add first Cognitive Core

Implemented the first standalone Cognitive Core slice.

FILES
-----
ai/cognitive_core.py
ai/prompts/cognitive_core_system_prompt.txt
tests/test_cognitive_core.py

ARCHITECTURE
------------
Cognitive Core performs one LLM cognitive turn and returns the existing
CognitiveTurnResult contract.

Input:
- current user message
- HumanModel
- previous ActiveConversationState
- relevant DialogueHistory
- Human Experience is explicitly disabled in this first slice

Relevant DialogueHistory consists of:
- recent dialogue window
- structurally referenced source messages from HumanModel / ACS
- deduplicated and ordered by sequence

The Cognitive Core:
- performs exactly one LLM call
- uses strict OpenAI Structured Outputs
- uses the existing CognitiveTurnResult
- does not persist state
- does not invoke DiscoveryStateApplier
- does not integrate with DialogEngine/runtime
- has no retry
- has no fallback

Python remains responsible for deterministic state application and persistent
contract enforcement outside the Cognitive Core.

STRICT STRUCTURED OUTPUT
------------------------
The Pydantic-generated CognitiveTurnResult JSON Schema required an API-facing
normalization for OpenAI strict Structured Outputs.

Every object schema property is presented as required while preserving the
underlying CognitiveTurnResult contract.

The source Pydantic schema is not mutated.

The complete normalized CognitiveTurnResult schema was verified against the
real OpenAI Structured Outputs API.

Result:
FULL_CURRENT_SCHEMA_API_GATE=PASSED

The earlier full-schema request failure could not be reproduced after
diagnostic isolation.

CONDITIONAL CONTRACT RULES
--------------------------
JSON Schema alone cannot express all cross-field invariants enforced by the
CognitiveTurnResult Pydantic validators.

The Cognitive Core system prompt therefore explicitly communicates the
important conditional rules, including:

- ItemOperation ADD / REINFORCE / CORRECT requirements
- RelationOperation requirements
- evidence/source span requirements
- existing/local reference exclusivity
- ProposalMaterialization conditional references
- reconciliation target cardinality
- ActionContent / ActionTarget reference rules
- ReplySegment realization linkage

The strict Pydantic contract remains unchanged and is still the final validator.

TIMEOUT DIAGNOSTIC
------------------
The first real CognitiveCore.propose() calls disappeared during the OpenAI
request.

Diagnostic isolation established:

- raw OpenAI request: PASS
- minimal Structured Output: PASS
- complete CognitiveTurnResult schema: PASS
- real Cognitive Core messages + complete schema without explicit per-request
  timeout: PASS

The confirmed suspect was the explicit 15-second per-request timeout.

The per-request timeout and the unused CognitiveCore timeout_seconds setting
were removed.

Timeout configuration is now the responsibility of the injected OpenAI client.

END-TO-END LIVE GATE
--------------------
A real CognitiveCore.propose() call using gpt-5-mini successfully completed:

user input
→ Cognitive Core input construction
→ system prompt
→ OpenAI Structured Output
→ JSON parsing
→ CognitiveTurnResult.model_validate()
→ valid CognitiveTurnResult

Observed result:

STATE_PATCH_ITEMS=2
STATE_PATCH_RELATIONS=0
RECONCILIATION_COUNT=0
ACTION_CONTENT_COUNT=1
ACTION_TARGET_COUNT=1
REPLY_SEGMENT_COUNT=1

A non-empty ReplySegment was produced.

A later UnicodeEncodeError occurred only in the temporary Windows diagnostic
harness while printing Russian reply text through cp1252, after propose()
had already successfully returned.

It is not a Cognitive Core failure.

VALIDATION
----------
Targeted Cognitive Core / CognitiveTurn / StateApplier tests:
49 passed

Full project test suite:
193 passed

py_compile:
PASSED

git diff --check:
PASSED

BOUNDARY
--------
This milestone proves that the first standalone Cognitive Core can produce a
technically valid CognitiveTurnResult through a real LLM call.

It does NOT yet prove consultation quality.

The Cognitive Core is NOT connected to production/runtime.

The next development stage is behavioral evaluation of Cognitive Core reasoning:
whether it understands the person correctly, chooses useful next actions,
maintains HumanModel / ACS appropriately, avoids questionnaire behavior, and
produces natural responses before runtime integration.
===============================================================================

================================================================================
COGNITIVE CORE — EVIDENCE BOUNDARY HARDENING COMPLETE
================================================================================

STATUS
------

Technical evidence boundary hardening for the standalone Cognitive Core is complete.

Commit:

d799783 Harden Cognitive Core evidence boundary

The commit was pushed to main.

The Cognitive Core remains standalone and is NOT connected to production runtime.


WHY THIS STAGE WAS NEEDED
-------------------------

Behavioral live testing exposed two technical contract failures.

1. LOCAL ID NAMESPACES

The LLM returned local identifiers such as:

ac1
rt_local_1

instead of the required contract namespaces:

item:
relation:
content:
target:
segment:

The prompt previously listed the namespaces but did not explicitly map every
field to its required namespace or prohibit aliases.

The Cognitive Core prompt was hardened so every local ID type has an explicit
namespace and all references must reuse the exact declared local ID.

This was independently audited against ai/cognitive_turn.py and accepted.


2. SOURCE SPAN OWNERSHIP

During the third behavioral turn the LLM repeatedly returned:

source_span = [0, 53)

for a USER message whose exact length was 52 characters.

This happened again even after the prompt explicitly instructed the model to
respect exact character offsets.

This demonstrated that exact character coordinate calculation is the wrong
responsibility for the LLM.

Architectural ownership was corrected:

LLM
    selects semantic evidence as an exact verbatim source_quote

Python
    deterministically converts source_quote into SourceSpan

The internal CognitiveTurnResult contract still uses numeric SourceSpan.
StateApplier and the persistent Data Model remain unchanged.


SOURCE_QUOTE EVIDENCE BOUNDARY
------------------------------

The API-facing Cognitive Core schema now uses source_quote instead of numeric
source_span in all three evidence paths:

- ItemOperation evidence
- RelationOperation evidence
- reconciliation evidence

The LLM must return an exact verbatim substring from the CURRENT USER message.

Python then:

1. verifies that the quote is non-empty where required;
2. searches the unchanged USER message for all exact occurrences;
3. accepts only one unique exact occurrence;
4. computes:

   char_start = exact occurrence position
   char_end   = char_start + len(source_quote)

5. creates the ordinary internal SourceSpan;
6. validates the resulting CognitiveTurnResult;
7. performs the existing numeric boundary validation.

No fuzzy matching is used.

No lowercase conversion, whitespace normalization, Unicode normalization,
punctuation repair or other heuristic correction is used.

If the quote:

- is missing when required;
- is empty;
- does not exist in the USER message;
- occurs more than once;

the Cognitive Core fails with:

CognitiveCoreError(category="validation_failure")

Python does not choose the first duplicate occurrence.

Overlapping duplicate occurrences are also detected.

There is no retry and no fallback.


DEFENSIVE VALIDATION
--------------------

The previous numeric SourceSpan boundary validation remains in CognitiveCore.

For every resulting SourceSpan:

0 <= char_start < char_end <= len(current_user_message.text)

StateApplier keeps its own defensive validation.

Persistent SourceReference validation also remains unchanged.

Therefore the new source_quote adapter does not replace existing protections;
it adds a deterministic evidence boundary before persistence.


LIVE GATE
---------

The new boundary was tested live on the same third behavioral turn that had
previously failed.

USER:

"Да просто жрать хочется постоянно. Особенно вечером."

Exact message length:

52

The live strict Structured Output request was accepted.

The LLM selected evidence through source_quote.

Python converted the selected evidence into:

[0, 52)

for:

- state_patch.item_operations[0].source_span
- state_patch.item_operations[1].source_span
- reconciliation[0].source_span

Each resulting span pointed exactly to:

"Да просто жрать хочется постоянно. Особенно вечером."

The returned CognitiveTurnResult passed validation.

Previous ACS target reconciliation passed.

All local references resolved.

The targeted ActionContent was realized by ReplySegment.

Exactly one API call was made.

No retry or fallback occurred.

Live gate verdict:

PASSED


TEST STATUS
-----------

After implementation:

Targeted tests:
100 passed

Full V2 test suite:
244 passed

py_compile:
PASS

git diff --check:
PASS


UNCHANGED BOUNDARIES
--------------------

The following were intentionally NOT changed:

- internal CognitiveTurnResult semantics
- SourceSpan internal model
- DiscoveryStateApplier
- persistent HumanModel
- persistent Relation model
- SourceReference
- DialogueHistory
- ACS semantics
- runtime / legacy flow
- dependencies

No new Engine was introduced.


ARCHITECTURAL PRINCIPLE CONFIRMED
---------------------------------

This stage reinforced the intended Hybrid architecture:

LLM owns:

- semantic understanding
- interpretation
- selection of meaningful evidence
- formulation
- decision
- natural response

Python owns:

- exact identifiers
- exact references
- exact character coordinates
- validation
- persistence invariants
- deterministic state transitions
- hard boundaries

The system must not force the LLM to perform deterministic bookkeeping that
Python can perform exactly.


BEHAVIORAL STATUS
-----------------

The technical Cognitive Core chain has now been demonstrated across consecutive
turns with:

HumanModel
→ ACS
→ next USER message
→ reconciliation
→ state patch
→ next system action

However, behavioral evaluation is NOT complete.

Across the first three behavioral turns the Cognitive Core repeatedly moved
toward investigating weight-loss attempts, hunger and possible eating triggers.

Observed trajectory:

USER problem / irritation with abdomen
→ previous attempts
→ why the attempt was difficult
→ physical hunger vs habit / emotional eating / boredom

This may indicate a behavioral tendency toward diagnostic questioning about
weight-loss causes instead of first developing a sufficiently meaningful
picture of what excess weight has changed in the person's life and why change
matters to that person now.

This is an observed behavioral issue, NOT yet a technical contract failure.

No behavioral correction was made during this technical stage.

The next development stage is behavioral evaluation of Cognitive Core reasoning
and dialogue decisions before runtime integration.

================================================================================
DISCOVERY — MECHANISM / EXPERTISE FUNCTION RESTORED
================================================================================

STATUS
------

Architecture clarification complete.

Research source of truth updated and committed:

    9142a7c Clarify Discovery expertise and sufficiency

Commit pushed to main.

This is NOT a new product direction.

It restores an important function of Discovery that existed in the intended
consultation model but had been underrepresented during formalization of the
Cognitive Cycle and Product Boundary.


WHAT WAS MISSING
----------------

During behavioral evaluation of the first Cognitive Core turns, Discovery was
being evaluated too strongly through one major product function:

    HUMAN / MEANINGFUL CHANGE

That function answers questions such as:

    What has the problem changed in this person's life?
    What does it mean to this person?
    What does the person want to regain or change?

Desired user effect:

    "Меня действительно поняли."

This remains essential.

However, Previous Attempts / Barriers have another independent product function
that must also be preserved:

    MECHANISM / EXPERTISE

The consultation should be capable of understanding enough of the person's
experienced problem mechanism to create a justified sense that the situation
is professionally recognizable.

Desired user effect:

    "Знакомо.
     Они понимают, что со мной происходит.
     Они понимают, почему мои предыдущие попытки не сработали.
     Похоже, они знают, как с такими ситуациями работать."

Compact internal formulation:

    "Знаем это. Знаем почему. Знаем как решить."

This does NOT authorize the AI to pretend certainty or knowledge that has not
actually been established.


TWO COMPLEMENTARY DISCOVERY LINES
---------------------------------

Discovery now explicitly recognizes at least two complementary lines of useful
understanding.

LINE A — HUMAN / MEANINGFUL CHANGE

May include:

    experience
    Before -> Now
    life changes
    limitations
    adaptations
    Personal Meaning
    Desired Change / Return

Primary product value:

    understand the person and what the problem has changed for them.


LINE B — MECHANISM / EXPERTISE

May include:

    Previous Attempts
    why an attempt was not sustainable in the user's experience
    substantial Barriers
    recurring mechanisms or patterns
    discriminating between materially different working explanations

Primary product value:

    understand the problem well enough for professional recognition.

Neither line is a required stage.

There is no mandatory order:

    A -> B

or:

    B -> A

There is no requirement for equal depth in every conversation.

Cognitive Core must compare the expected value of available Decisions in the
specific conversation.


EPISTEMIC BOUNDARY
------------------

Expert Recognition is NOT Objective Diagnosis.

The user's subjective experience is sufficient for a working model.

The system may understand and test working explanations without claiming that
it has established an objective physiological, psychological, behavioral, or
medical cause.

A hypothesis remains a hypothesis.

A user-supported interpretation remains provenance-aware.

Expertise must not be simulated by artificial certainty.


VALUE / SUFFICIENCY CLARIFICATION
---------------------------------

The previous practical question:

    "Will this answer teach us something new?"

is too weak.

Almost any additional question can produce new information.

The opposite restriction:

    "Is this information needed for Meaningful Change Picture?"

is also too narrow.

A question can have legitimate value because it materially improves
Mechanism / Expertise understanding.

The broader criterion is:

    Will the next answer materially improve useful understanding of the person
    OR a substantial mechanism of the person's problem within the current
    product boundary?

A branch should stop when further detail has low expected value for current
Discovery compared with another available Decision.

Further detail must not be collected merely because it could be useful to a
future specialist or future solution design.


BOUNDARY WITH SOLUTION DESIGN
-----------------------------

Useful Mechanism / Expertise exploration may:

    distinguish materially different working explanations;
    clarify why a previous attempt was not sustainable;
    identify a substantial recurring Barrier;
    improve the accuracy of Reflection;
    improve the next Discovery Decision;
    create justified professional recognition.

It becomes excessive diagnostic exploration when additional detail:

    barely changes the working Formulation;
    is collected mainly to select or tune a future solution;
    attempts to establish an objective medical / physiological /
    psychological cause;
    continues an already sufficient branch merely because more information
    can be obtained;
    has lower expected value than another useful Discovery Decision.

The boundary is semantic and value-based.

It is NOT defined by keywords, a fixed number of questions, required fields,
or a hardcoded route.


BEHAVIORAL AUDIT — REVISED VERDICT
----------------------------------

Observed Cognitive Core trajectory:

TURN 1

USER:

    "Последние пару лет набрал вес, и больше всего меня раздражает живот."

CORE:

    "Понял — вы набрали вес за последние пару лет, и больше всего вас
     раздражает живот. Что вы уже пробовали, чтобы с этим справиться?"

Decision:

    Stated Problem -> Previous Attempt

Revised evaluation:

    ACCEPTABLE

Previous Attempts did not have to wait for Personal Meaning.
Opening the Mechanism / Expertise line is a legitimate Decision.


TURN 2

USER:

    "Да особо ничего. Пытался меньше есть, но надолго меня не хватает."

CORE:

    "Понял. Что именно мешало выдерживать уменьшение еды надолго —
     сильный голод, стресс и занятость, привычки или что-то другое?"

Decision:

    Previous Attempt -> Barrier

Revised evaluation:

    ACCEPTABLE AND VALUABLE

The answer can materially improve understanding of why the user's previous
attempt was not sustainable.


TURN 3

USER:

    "Да просто жрать хочется постоянно. Особенно вечером."

CORE:

    "Понял — особенно вечерами. Это больше физический голод
     (реально голодны) или скорее привычка/эмоционально
     (скука, стресс)?"

Decision:

    Barrier -> discriminating mechanism question

Revised evaluation:

    ACCEPTABLE AS ONE DISCRIMINATING STEP

Distinguishing substantially different experienced mechanisms can improve
professional understanding even before any solution is selected.

However, this turn is now at the SUFFICIENCY BOUNDARY.

After the answer, Cognitive Core should again compare the expected value of
continuing Mechanism / Expertise with other useful Decisions such as:

    Personal Meaning
    Desired Change
    Reflection
    another meaningful area
    Transition, when appropriate

A continued sequence of increasingly detailed nutrition / behavior questions
would require progressively stronger justification.


OVERALL BEHAVIORAL VERDICT
--------------------------

The observed three-turn trajectory is:

    ACCEPTABLE SO FAR, BUT AT THE SUFFICIENCY BOUNDARY.

The three turns do NOT by themselves demonstrate an incorrect diagnostic
funnel.

They form a compact expert-understanding chain:

    previous attempt
        ->
    lack of sustainability
        ->
    substantial recurring mechanism

The next Cognitive Core Decision will be particularly informative for
behavioral evaluation.


RESEARCH UPDATED
----------------

The clarification was integrated only where it represented a real semantic
gap.

Updated:

    Research/Human_Experience/Weight/00_INDEX.md
    Research/Human_Experience/Weight/00_INDEX_RU.md
    Research/Human_Experience/Weight/08_PREVIOUS_ATTEMPTS_BARRIERS.txt
    Research/Human_Experience/Weight/11_BOUNDARIES.txt
    Research/Human_Experience/Weight/12_Cognitive_Cycle.txt

Not changed:

    09_REFLECTION_AWARENESS.txt
    10_TRANSITION_TO_HUMAN_CONSULTANT.txt
    14_Human_Model.txt

Those documents already support Previous Attempts / Barriers within their
existing responsibilities.


NEXT
----

Do NOT patch Cognitive Core prompt using the earlier restrictive behavioral
recommendation.

The source of truth has changed enough that the prompt must be evaluated
against the restored two-line Discovery model first.

Next behavioral / engineering work should preserve BOTH:

    HUMAN / MEANINGFUL CHANGE

and:

    MECHANISM / EXPERTISE

while strengthening Value / Sufficiency rather than hardcoding a route.
=============================================================================================

=============================================================================================

COGNITIVE CORE — CONSENT / DECLINED CONTRACT HARDENING

Первый полный end-to-end Conversation Lab run дошёл до шестого пользовательского хода и выявил технический blocker на границе CognitiveTurnResult → DiscoveryStateApplier.

Сценарий:
SYSTEM_PROPOSAL:
«Хотите обсудить варианты или пока не менять ничего?»

Следующий USER:
«Пока не хочу ничего пробовать.»

Предыдущий ResponseTarget был ошибочно сформирован Cognitive Core как OPEN_RESPONSE, хотя по смыслу системное действие запрашивало принятие или 
отклонение предлагаемого действия и должно было иметь interaction=CONSENT.

Естественный outcome DECLINED поэтому образовывал несовместимую пару:

OPEN_RESPONSE → DECLINED

и DiscoveryStateApplier корректно атомарно отвергал turn.

Offline audit подтвердил:
- контракт уже способен выразить естественный сценарий как CONSENT → DECLINED;
- расширять DECLINED на OPEN_RESPONSE неправильно;
- StateApplier validation ослаблять не требуется;
- первичная проблема находилась в семантическом моделировании SystemAction / ResponseTarget;
- дополнительная причина — недостаточно явная семантика interaction/outcome в Cognitive Core prompt.

Исправление:
- в Cognitive Core system prompt явно определены OPEN_RESPONSE, CLARIFICATION, EVALUATION и CONSENT;
- запрос принятия/отклонения конкретного действия, перехода или следующего шага должен использовать CONSENT;
- CONSENTED / DECLINED относятся к CONSENT;
- REFUSED не используется как замена DECLINED для обхода compatibility validation;
- добавлены focused tests на корректный CONSENT → DECLINED и запрещённый OPEN_RESPONSE → DECLINED;
- StateApplier compatibility matrix не изменялась.

Проверки:
- focused suite: 103 passed;
- full suite: 247 passed;
- py_compile: PASS;
- git diff --check: PASS.

Commit:
c318ed4 Clarify Cognitive Core consent semantics

Следующий этап:
повторный полный end-to-end Conversation Lab run текущего Cognitive Core без ручного вмешательства между ходами. 
Цель — проверить полный маршрут Discovery после устранения технического blocker и отдельно оценить уже обнаруженные behavioral issues: 
баланс Human / Meaningful Change и Mechanism / Expertise, Value / Sufficiency, преждевременный solution design, Reflection и 
естественный Transition / Stop.
=======================================================================================================================================

=======================================================================================================================================

COGNITIVE CORE — VALUE / SUFFICIENCY AND PRODUCT BOUNDARY HARDENING

Второй полный end-to-end Conversation Lab после исправления CONSENT / DECLINED технически завершился успешно:

- 9 Cognitive Core calls;
- все CognitiveTurnResult валидны;
- все DiscoveryStateApplier applications успешны;
- финальный ACS=None;
- предыдущий технический blocker устранён.

Behavioral audit выявил устойчивый drift:

Stated Problem
→ Previous Attempts
→ Barrier / Mechanism
→ concrete behavioral experiment
→ refusal
→ alternative proposal
→ future-return proposal
→ Stop.

Human / Meaningful Change почти не исследовалась, Reflection отсутствовала.

Главный архитектурный вывод:
проблема не в выборе Mechanism / Expertise как линии Discovery.
Human / Meaningful Change и Mechanism / Expertise остаются двумя полноценными
линиями без обязательного порядка и одинаковой глубины.

Root cause:
утверждённая в Research семантика Value / Sufficiency и продуктовой границы
была недостаточно полно перенесена в runtime Cognitive Core prompt.

Локальная понятность одного механизма не означает готовность перейти к решению.

Перед следующим действием Cognitive Core должен динамически сравнивать
ожидаемую ценность допустимых действий внутри текущей продуктовой границы.

Новый Discovery question оправдан, только если ответ способен существенно
изменить:
- полезное понимание человека;
- существенный механизм проблемы;
- следующий допустимый Decision.

При снижении дополнительной ценности текущей ветки Core должен сравнить её
с другими допустимыми кандидатами:
- другой Discovery-линией;
- Reflection;
- Transition;
- Respect Pause / Refusal;
- Stop Exploration.

Отдельно зафиксирована product eligibility boundary.

Текущий Discovery:
понять человека
→ отразить рабочую картину
→ понять значимые Previous Attempts / Barriers / Mechanisms
→ показать ценность следующего шага
→ Transition к живому консультанту с meaningful consent.

Concrete behavioral experiment, recommendation, plan, practice и подбор
конкретного решения не являются обычными действиями текущего Discovery.

Понятность локального механизма сама по себе не разрешает concrete solution.

SYSTEM_PROPOSAL как action kind не запрещён.
Он остаётся допустимым для содержательных действий внутри Discovery,
например проверяемой системной версии.
Ограничение относится к семантическому содержанию и продуктовой функции,
а не к enum.

Reflection остаётся value-based candidate, а не обязательным ритуалом.

После DECLINED:
- отклонённое действие теряет ценность;
- соседний вариант того же решения не должен предлагаться автоматически;
- Respect Pause / Refusal и Stop Exploration получают высокий приоритет;
- причина отказа исследуется только если это существенно меняет допустимый
  Decision внутри Discovery и не создаёт давления.

Отказ от system proposal сам по себе не является BARRIER исходной проблемы.
Reconciliation уже способен хранить факт отклонения предложения.

Новые persistent state, field, score, Engine или slot architecture не нужны.

Реализация:
изменены только:
- ai/prompts/cognitive_core_system_prompt.txt
- tests/test_cognitive_core.py

Добавлен runtime prompt block:
VALUE / SUFFICIENCY AND PRODUCT ELIGIBILITY.

Добавлены focused prompt-contract tests.

Не изменялись:
- Research;
- CognitiveTurn contract;
- DiscoveryStateApplier;
- Data Contracts;
- runtime integration.

Проверки:
- focused Cognitive Core: 66 passed;
- full pytest: 250 passed;
- git diff --check: PASS.

Commit:
4b2363b Enforce Cognitive Core discovery boundary

Следующий этап:
новый полный end-to-end Conversation Lab текущего Cognitive Core.
Цель — проверить, изменился ли реальный Decision trajectory:
не уходит ли Core преждевременно в solution design,
умеет ли покидать достаточную Mechanism-ветку,
возникает ли Human / Meaningful Change,
появляется ли Reflection,
и корректно ли обрабатываются Refusal / Transition / Stop.

================================================================================
COGNITIVE CORE — MINIMAL DECISION CONTRACT
================================================================================

После полного Conversation Lab и последующего offline architecture audit
было установлено, что Value / Sufficiency и Product Boundary существовали
в runtime прежде всего как инструкции Cognitive LLM, но выбранный Decision
не имел явного представления в CognitiveTurnResult.

До изменения runtime имел вид:

INPUT
→ one Cognitive LLM call
→ CognitiveTurnResult {
    state_patch,
    reconciliation,
    system_action,
    reply_segments
  }
→ DiscoveryStateApplier.

Из system_action можно было лишь косвенно восстанавливать выбранное действие.
Python не имел явной информации о том, продолжает ли Core Human Discovery,
Mechanism Discovery, делает Reflection, Recognition, Transition,
уважает отказ, завершает Discovery или отвечает на прямой вопрос.

Принято архитектурное решение:
Decision должен стать минимальной ephemeral частью CognitiveTurnResult,
не создавая отдельного DecisionEngine и не сохраняя reasoning.

Добавлен обязательный DecisionIntent:

- HUMAN_DISCOVERY
- MECHANISM_DISCOVERY
- REFLECTION
- RECOGNITION
- TRANSITION
- RESPECT_PAUSE_OR_REFUSAL
- STOP_EXPLORATION
- ANSWER_USER_QUESTION

Добавлено обязательное поле:

CognitiveTurnResult.decision_intent

DecisionIntent:
- создаётся тем же единственным Cognitive LLM call;
- выбирается до Response;
- описывает функцию выбранного действия Cognitive Cycle, а не тему текста;
- ephemeral;
- не сохраняется в HumanModel;
- не сохраняется в ACS;
- не сохраняется в DialogueHistory;
- не содержит reasoning, rationale или score.

Добавлены coarse deterministic invariants:

- REFLECTION требует SYSTEM_REFLECTION;
- RECOGNITION требует RECOGNITION_OPTION;
- TRANSITION требует SYSTEM_TRANSITION;
- target для SYSTEM_TRANSITION требует CONSENT;
- STOP_EXPLORATION не создаёт ResponseTarget;
- HUMAN_DISCOVERY и MECHANISM_DISCOVERY требуют ResponseTarget;
- RESPECT_PAUSE_OR_REFUSAL не открывает новый OPEN_RESPONSE/CLARIFICATION;
- CONSENT может адресовать только SYSTEM_PROPOSAL или SYSTEM_TRANSITION;
- SYSTEM_REFLECTION не может быть предметом CONSENT;
- корректный owner/subject lifecycle из предыдущего hardening сохранён.

Сознательно НЕ реализованы:

- semantic Product Boundary classifier;
- keyword/phrase matching;
- Python-интерпретация semantic_content;
- автоматическое различение Human и Mechanism по словам;
- rejected-action registry;
- общий запрет BARRIER после DECLINED;
- reasoning/rationale;
- expected-value/sufficiency scores;
- persistent Decision;
- дополнительный LLM call;
- retry/fallback.

ВАЖНО:
Minimal Decision Contract не решает semantic Product Boundary полностью.

LLM теоретически всё ещё может объявить
decision_intent = MECHANISM_DISCOVERY,
но фактически сформулировать concrete experiment/practice.

Python намеренно не пытается обнаруживать это по ключевым словам.
Семантическое соответствие declared Decision реальному содержанию действия
остаётся ответственностью Cognitive LLM и предметом Conversation Lab /
дальнейшего semantic boundary design.

Таким образом граница HYBRID архитектуры стала явнее:

LLM:
понимает человеческий смысл и выбирает semantic Decision.

Python:
проверяет contract, structural consistency, state boundary
и coarse architectural invariants.

Persistent Discovery Data Model не изменён.
Production DiscoveryStateApplier не изменён.
CognitiveCore по-прежнему использует один LLM call.

Проверки:
- focused tests: 138 passed;
- full pytest: 282 passed;
- py_compile: PASS;
- git diff --check: PASS.

Commit:
059f180 Add Cognitive Core decision intent contract

Следующий этап:
после фиксации истории провести новый полный Conversation Lab
с реальным CognitiveCore и DiscoveryStateApplier и проверить,
изменило ли явное DecisionIntent фактическое поведение Core,
особенно Value / Sufficiency, баланс Human/Mechanism,
Product Boundary, Reflection и обработку отказа.
================================================================================
================================================================================
COGNITIVE CORE — OPERATIONAL DECISION POLICY
================================================================================

После внедрения Minimal Decision Contract был проведён полный Conversation Lab
на commit 6c3bf73.

Технический результат:
- 19 последовательных live Cognitive Core вызовов;
- 19 валидных CognitiveTurnResult;
- 0 retry/fallback;
- 0 contract/state failures;
- естественная остановка после просьбы пользователя.

Minimal Decision Contract выполнил свою задачу:
decision_intent стал наблюдаемым и соответствовал фактическому типу действия.

При этом behavioral verdict Conversation Lab: FAIL.

Основная обнаруженная проблема:
Core систематически выбирал MECHANISM_DISCOVERY после того,
как marginal value этой ветки уже стала низкой.

Decision trajectory:

HUMAN_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ MECHANISM_DISCOVERY
→ MECHANISM_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ REFLECTION
→ MECHANISM_DISCOVERY
→ MECHANISM_DISCOVERY
→ MECHANISM_DISCOVERY
→ RESPECT_PAUSE_OR_REFUSAL
→ RESPECT_PAUSE_OR_REFUSAL

Распределение:
- HUMAN_DISCOVERY: 1
- MECHANISM_DISCOVERY: 10
- REFLECTION: 6
- RESPECT_PAUSE_OR_REFUSAL: 2

Положительные изменения относительно предыдущего Lab:
- исчезла физиологическая ветка fat/bloating/diastasis;
- исчезла домашняя self-check;
- исчез premature solution design;
- Product Boundary не нарушался;
- после refusal отклонённое действие не повторялось;
- refusal не материализовался как BARRIER;
- остановка без обязательного Transition прошла корректно.

Главные behavioral проблемы:
1. Value/Sufficiency не управлял выбором следующего действия достаточно устойчиво.
2. Human / Meaningful Change практически не исследовалась.
3. REFLECTION преимущественно использовалась как local confirmation,
   а не как сборка целостной working picture.

Offline design audit установил:

PRIMARY ROOT CAUSE:
DECISION POLICY GAP.

Secondary:
- PROMPT TRANSFER GAP;
- частичный STATE VISIBILITY GAP.

Не обнаружено необходимости менять:
- DecisionIntent contract;
- Discovery Data Model;
- persistent state;
- StateApplier;
- количество LLM calls.

Ключевой вывод аудита:

UNKNOWN является полноценным Discovery result.

Подтверждённое:
«не знаю» / «не наблюдал» / «не могу определить»
может завершать semantic axis.

UNKNOWN не означает:
«задать тот же вопрос другими словами».

Mechanism Sufficiency в исследованном диалоге была достигнута
не позднее USER turn 6:

Attempt:
меньше есть

→ Barrier:
сильное вечернее желание поесть

→ Result:
ограничение невозможно долго поддерживать

→ Uncertainty:
пользователь не знает, физический это голод или привычка.

Дополнительные сведения могут быть полезны,
но их возможность сама по себе не оправдывает продолжение ветки.

Архитектурное решение:

Не добавлять:
- fixed Human → Mechanism flow;
- обязательное чередование линий;
- лимиты количества вопросов;
- numeric Value scores;
- slots/required fields;
- saturation counters;
- keyword rules;
- deterministic Python semantic policy;
- отдельный DecisionEngine;
- второй LLM call;
- persistent Decision/Value/Sufficiency;
- chain-of-thought.

Вместо этого усилить существующий один Cognitive LLM call
операциональной процедурой выбора следующего DecisionIntent.

Реализация:

В cognitive_core_system_prompt.txt добавлен
OPERATIONAL CANDIDATE-SELECTION PROTOCOL.

Перед окончательным выбором decision_intent Cognitive Core должен:

1. INTEGRATE CURRENT RESULT
   Определить фактический результат текущей реплики,
   включая supported uncertainty и отсутствие доступного знания.

2. CLOSE EXHAUSTED SEMANTIC AXIS
   Не продолжать ту же semantic axis,
   если следующий вопрос только дробит уже подтверждённую uncertainty
   и не меняет working picture или следующий допустимый Decision.

3. GENERATE DISTINCT ELIGIBLE CANDIDATES
   Рассмотреть содержательно различные уместные Cognitive actions.

4. COMPARE EXPECTED VALUE
   Сравнить, что каждый кандидат реально способен изменить,
   и выбрать наиболее ценный следующий Cognitive action,
   а не просто следующий локально логичный вопрос.

5. COMPARE DISCOVERY LINES
   Human / Meaningful Change и Mechanism / Expertise
   не имеют фиксированного порядка, квот или обязательного чередования.
   Уже глубокая линия не должна продолжаться только потому,
   что в ней можно получить ещё детали.

6. REFLECTION DISTINCTION
   Различать:
   - local confirmation / repair;
   - multi-element working-picture Reflection.

   REFLECTION не должна автоматически означать
   пересказ последней пользовательской реплики.

7. PRODUCT BOUNDARY
   Детали, полезные преимущественно для solution design,
   plan selection, home diagnostics или objective diagnosis,
   не оправдывают продолжение Discovery.

Persistent Data Model не изменён.
DecisionIntent contract не изменён.
Python runtime не изменён.
Research не изменён.
Один Cognitive LLM call сохранён.

Изменены только:
- ai/prompts/cognitive_core_system_prompt.txt
- tests/test_cognitive_core.py

Проверки:
- Cognitive Core focused tests: 72 passed
- full pytest: 283 passed
- py_compile tests/test_cognitive_core.py: PASS
- git diff --check: PASS

Commit:
43acc50 Improve Cognitive Core decision policy

Следующий этап:
полный live Conversation Lab на новой Decision Policy
с тем же синтетическим профилем для прямого сравнения
с предыдущими behavioral runs.

================================================================================
COGNITIVE CORE — REFERENCE OWNERSHIP HARDENING
================================================================================

КОНТЕКСТ

После внедрения Operational Decision Policy был проведён новый полный
Conversation Lab на commit:

eb5f764 Document Cognitive Core decision policy

Поведенчески первые пять успешных ходов показали существенное улучшение:

- HUMAN_DISCOVERY начал реально развиваться;
- появились multi-element working-picture Reflection;
- преждевременный Mechanism route не возник;
- Product Boundary сохранялся;
- вопросы имели заметно более высокую ожидаемую ценность.

Однако Turn 6 завершился техническим failure до StateApplier:

CognitiveCoreError
category=validation_failure

Pydantic cause:

ActionContent references an unknown local item.

USER turn:

«Да, так.»

Structured Output API:
PASS

JSON parsing:
PASS

Source quote adapter:
PASS до Pydantic

CognitiveTurnResult validation:
FAIL

StateApplier:
не вызван

Persistent state:
не изменён

Retry/fallback:
не выполнялся

Поэтому полный behavioral regression gate Operational Decision Policy
не был завершён.

────────────────────────────────────
OFFLINE CONTRACT-BOUNDARY AUDIT
────────────────────────────────────

После failure был проведён отдельный offline audit без изменений кода.

Установленный data path:

OpenAI Structured Output
→ JSON parsing
→ source_quote adaptation
→ CognitiveTurnResult.model_validate()
→ CognitiveTurnResult cross-reference validation
→ DiscoveryStateApplier

Ошибка была обнаружена на границе CognitiveTurnResult до StateApplier.

Внутренний CognitiveTurn contract уже выражал правильную семантику ссылок.

Local namespaces текущего CognitiveTurnResult включают:

item:*
relation:*
content:*
target:*
segment:*

Persistent model references используют:

mi_*
rel_*
aci_*
rt_*

Для ActionContent.model_item_refs допустимы:

1. existing persistent HumanModel item:

existing_item_id = exact mi_*
local_item_id = null

2. current-turn local item:

local_item_id = exact item:*
existing_item_id = null

При этом local item обязан быть объявлен ADD или CORRECT ItemOperation
текущего StatePatch.

Аналогичное правило действует для relation references.

Local alias не является сокращённым именем существующего persistent object.

────────────────────────────────────
ROOT CAUSE
────────────────────────────────────

PRIMARY:

STOCHASTIC CONTRACT VIOLATION

LLM сгенерировала local item reference, который не был объявлен
текущим StatePatch.

Существующий Pydantic validator корректно отверг результат.

Secondary:

1. API SCHEMA AMBIGUITY

Wire schema обеспечивает shape Structured Output, но не может гарантировать
динамическое membership-условие:

local reference
MUST belong to
local declarations текущего result.

2. PROMPT REFERENCE RULE GAP

Общие правила local references уже существовали, но confirmation/reflection
сценарий без нового state не был сформулирован достаточно операционально.

3. OBSERVABILITY GAP

При validation_failure текущий CognitiveCoreError не сохраняет raw structured
payload, поэтому exact offending local reference после Lab установить
не удалось.

Observability признана отдельной задачей и в текущий fix не включалась.

────────────────────────────────────
АРХИТЕКТУРНОЕ РЕШЕНИЕ
────────────────────────────────────

Не менять:

- CognitiveTurn internal Data Contract;
- persistent Data Model;
- DiscoveryStateApplier;
- source_quote adapter;
- one-LLM-call architecture;
- DecisionIntent;
- Operational Decision Policy;
- runtime state;
- retry/fallback.

Не выполнять semantic normalization:

item:* → mi_*

Python не должен угадывать, какой persistent semantic object имела в виду
модель под неизвестным local alias.

Невалидный CognitiveTurnResult должен по-прежнему fail closed.

Выбран минимальный generation-reliability hardening:

PROMPT REFERENCE OWNERSHIP CLARIFICATION
+
DIRECT CONTRACT TEST COVERAGE

────────────────────────────────────
РЕАЛИЗАЦИЯ
────────────────────────────────────

Изменены только:

ai/prompts/cognitive_core_system_prompt.txt
tests/test_cognitive_core.py
tests/test_cognitive_turn_contract.py

Prompt теперь явно требует:

EXISTING PERSISTENT REFERENCES

Если item/relation уже существует в переданном HumanModel:

- использовать точный persistent ID;
- item → existing_item_id = exact mi_*;
- relation → existing_relation_id = exact rel_*;
- соответствующий local_*_id = null.

Запрещено создавать новый local alias для существующего persistent object.

CURRENT-TURN LOCAL REFERENCES

local_item_id допустим только если exact item:* объявлен
ADD или CORRECT ItemOperation текущего StatePatch.

local_relation_id допустим только если exact relation:* объявлен
ADD или CORRECT RelationOperation текущего StatePatch.

CONFIRMATION / REFLECTION WITHOUT NEW STATE

Если turn подтверждает или отражает уже накопленную картину
без создания нового ModelItem/Relation:

- не создавать искусственный local alias;
- при необходимости использовать существующий mi_*/rel_*;
- если semantic reference не нужен, refs list может быть пустым.

NO GUESSING / NO NORMALIZATION

Python не угадывает значение неизвестного local alias.

Каждый emitted local reference обязан разрешаться через declarations
этого же CognitiveTurnResult.

────────────────────────────────────
TEST COVERAGE
────────────────────────────────────

Добавлены прямые positive/negative tests для:

ITEM REFERENCES

- unknown local item → FAIL;
- valid declared local item → PASS;
- existing persistent mi_* → PASS;
- mixed persistent + valid local → PASS;
- reflection/confirmation без новых item operations
  с existing mi_* → PASS;
- reflection/confirmation без новых item operations
  с invented item:* → FAIL.

RELATION REFERENCES

- valid declared local relation → PASS;
- unknown local relation → FAIL;
- existing persistent rel_* → PASS;
- mixed persistent + valid local relation → PASS.

Также добавлен focused prompt-contract test,
проверяющий существенные reference ownership instructions
без привязки ко всему тексту prompt.

────────────────────────────────────
ПРОВЕРКИ
────────────────────────────────────

CognitiveTurn contract:

53 passed

CognitiveCore:

73 passed

Совместный focused-прогон:

126 passed

Полный pytest:

295 passed in 27.76s

py_compile:
PASS

git diff --check:
PASS

Live API:
не запускался

Conversation Lab:
не запускался

────────────────────────────────────
COMMIT
────────────────────────────────────

d0b4b62 Harden Cognitive Core reference ownership

Commit отправлен в main.

────────────────────────────────────
ТЕКУЩЕЕ СОСТОЯНИЕ
────────────────────────────────────

Generation reliability для local/persistent reference ownership усилена.

Fail-closed semantic cross-reference validation сохранена.

Persistent Data Model не изменён.

CognitiveTurn production contract не изменён.

StateApplier не изменён.

One-LLM-call architecture сохранена.

Operational Decision Policy не изменена.

Следующий этап:

повторный полный live Conversation Lab на том же синтетическом профиле
для завершения behavioral regression gate Operational Decision Policy
и проверки, устранён ли Turn 6 reference failure.