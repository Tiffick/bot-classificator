===============================================================================
SPRINT 001
Дата: 2026-07-14

Название:
Восстановление dialog_engine.py

Что сделано:
- Исправлен синтаксис.
- Восстановлена логика update.
- Проверена компиляция.
- Проверен запуск проекта.
- Изменения отправлены в GitHub.

Коммит:
83b9832
Fix dialog_engine syntax and restore update flow

Статус:
✅ Завершено
===============================================================================

=========================================================
Дата: __________

Этап: Начало реализации новой архитектуры.

Что сделано:

- Создана папка ai/engines.
- Создан первый архитектурный модуль HumanModelEngine.
- Создан класс HumanModelEngine как каркас будущего движка.
- Создан файл human_model.py как будущая структура данных Human Model.
- dialog_engine.py подключён к HumanModelEngine без изменения поведения системы.
- Выполнена проверка синтаксиса.
- Выполнен запуск проекта.
- Подтверждено, что бот продолжает работать корректно.
- Изменения зафиксированы в Git.
- Выполнен commit.
- Выполнен push в GitHub.

Архитектурные решения:

- Отказались от идеи создавать все движки заранее.
- Принято решение создавать новый движок только в момент появления у него первой реальной ответственности.
- Определено, что Human Model станет центральной моделью понимания человека.
- Завершён этап архитектурного проектирования ядра цифрового консультанта.

Статус проекта:

Рабочее состояние.
Все изменения успешно сохранены в GitHub.
=========================================================

=========================================================
Дата: __________

Этап: Начало построения Human Model.

Что сделано:

- Создана структура данных HumanModel на основе dataclass.
- В HumanModel добавлены разделы:
  - facts
  - unknown
  - hypotheses
  - confidence
  - pain_map
  - motivation
  - consultation_stage
- HumanModelEngine переведён с возврата dict на возврат объекта HumanModel.
- HumanModelEngine получил первую реальную ответственность:
  построение начальной модели человека.
- Первичные факты из profile теперь помещаются в HumanModel.facts.
- Вызов HumanModelEngine перенесён в логически более правильное место внутри dialog_engine.
- После каждого изменения выполнен запуск проекта.
- Подтверждено, что бот продолжает работать без изменений.
- Изменения зафиксированы в Git.
- Выполнен commit.
- Выполнен push в GitHub.

Архитектурные решения:

- HumanModel больше не является заглушкой.
- HumanModel становится отдельной рабочей сущностью, а не копией profile.
- Profile остаётся источником данных.
- HumanModel становится рабочим представлением понимания человека.
- В дальнейшем HumanModel будет содержать не только факты, но и интерпретации, гипотезы, мотивацию и карту боли.

Статус проекта:

Рабочее состояние.
Все изменения успешно сохранены в GitHub.
=========================================================

=========================================================
Дата: __________

Этап: Завершение архитектурного проектирования V2.

Что сделано:

- Создана отдельная архитектурная спецификация V2.
- Полностью переработан документ "03_План рефакторинга".
- Полностью переработан документ "04_Human Model".
- Полностью переработан документ "05_Поток обработки сообщения".
- Завершена переработка архитектуры вокруг Human Model.
- Human Model утверждена как центральный объект системы.
- Определён окончательный конвейер обработки сообщений:
  Semantic → HumanModel → Reasoning → Decision → Impact → Emotional → Response Generator → Memory Update.
- Проверена согласованность документов V2.
- Подтверждена готовность архитектуры к началу реализации.

Архитектурные решения:

- Архитектура V2 утверждена как основное направление развития проекта.
- Завершён этап архитектурного проектирования.
- Дальнейшее развитие проекта переносится из области проектирования в область реализации.
- Все последующие изменения должны выполняться небольшими завершёнными итерациями с обязательной проверкой и фиксацией в Git.

Статус проекта:

Архитектура V2 утверждена.
Проект готов к началу реализации новой архитектуры.

=========================================================

=========================================================
Дата: 2026-07-23

Этап: Начало реализации архитектуры V2.

Что сделано:

- Создан новый модуль SemanticEngine.
- Логика определения известных и неизвестных данных пользователя вынесена из dialog_engine.py.
- Удалена устаревшая функция build_known_unknown().
- SemanticEngine стал первым самостоятельным Engine новой архитектуры.
- dialog_engine начал выполнять роль координатора, а не хранить семантическую логику.
- Проведено тестирование после рефакторинга.
- Поведение бота не изменилось.

Git:

Commit:
Extract SemanticEngine from dialog engine

Результат:

Начата практическая реализация архитектуры V2.
Первый Engine успешно выделен без изменения поведения системы.

=========================================================

=========================================================
Дата: 2026-07-24

Этап: HumanModel становится носителем семантического состояния.

Что сделано:

- dialog_engine перестал напрямую использовать SemanticEngine.
- Создание HumanModel полностью перенесено в HumanModelEngine.
- HumanModel начал содержать:
    - facts
    - known
    - unknown
- known и unknown теперь поступают из SemanticEngine через HumanModelEngine.
- dialog_engine использует HumanModel как единственный источник семантической информации.
- Исправлен тип поля unknown (list вместо dict).
- Удалён временный отладочный вывод HumanModel.

Git:

Commit:
Move semantic state into HumanModel

Результат:

Сделан ещё один шаг к архитектуре V2.
dialog_engine постепенно превращается в координатор,
а логика понимания пользователя концентрируется в HumanModel.

=========================================================

=============================

## 2026-07-25

### Рефакторинг: перенос применения обновлений профиля

Коммит:
106e638 — Move profile update filtering into HumanModelEngine

Что изменено:
- Из dialog_engine.py удалена логика фильтрации обновлений профиля.
- Добавлен метод HumanModelEngine.apply_update().
- Правила неизменяемых полей (name, gender) перенесены в HumanModelEngine.

Причина:
DialogEngine должен координировать диалог, а не содержать бизнес-логику изменения профиля.

Результат:
- уменьшена связность DialogEngine;
- логика обновления профиля сосредоточена в одном месте;
- поведение бота не изменилось (проверено запуском).

=============================

=============================

Дата: 26.07.2026

Тема:
Перенос логики определения завершённости знакомства в HumanModelEngine.

Что сделано:

- добавлен метод HumanModelEngine.is_discovery_complete(profile)
- DialogEngine больше не содержит собственной логики определения discovery_complete
- DialogEngine теперь делегирует эту задачу HumanModelEngine
- удалена устаревшая функция calculate_discovery_complete()

Результат:

DialogEngine стал ещё ближе к роли оркестратора, а логика работы с HumanModel сосредотачивается в HumanModelEngine. Это уменьшает связанность компонентов и упрощает дальнейшее развитие архитектуры.

Git:
Commit: 4a298ff
Move discovery completion logic into HumanModelEngine

=============================

=============================

Reuse HumanModelEngine instance

Что сделано:
- создан один экземпляр HumanModelEngine внутри run_dialog_engine()
- build(), apply_update() и is_discovery_complete() теперь используют один engine
- устранено повторное создание объекта

Результат:
- DialogEngine стал чище
- код готов к дальнейшему развитию HumanModelEngine
- поведение полностью сохранено

Git:
e7cebfe
Reuse HumanModelEngine instance in DialogEngine

=============================

=============================

### Рефакторинг №2 — завершён

Дата: 30.07.2026

Что сделано

- Вынесена логика сохранения истории диалога в отдельную функцию `append_history()`.
- `run_dialog_engine()` больше не занимается ручным добавлением сообщений в историю, а использует вызов:
  `append_history(history, user_text, reply)`

Проверка

- ✅ Код успешно запускается.
- ✅ История диалога сохраняется корректно.
- ✅ Ответ GPT не изменился.
- ✅ `discovery_complete` работает без изменений.
- ✅ Поведение программы полностью сохранено.

Git

Коммит:
Extract append_history helper

Hash:
0f5829d

Новые правила процесса

- Один ответ ChatGPT = один шаг.
- Один законченный рефакторинг = один коммит = один push.
- После каждого push — запись в журнал.
- Любая инструкция по изменению кода должна однозначно указывать изменяемый блок с помощью уникального окружающего контекста.

=============================

================================

### Рефакторинг №3 — завершён

Дата: 30.07.2026

Что сделано

- Перестроен конвейер обработки сообщений в соответствии с архитектурой V2.
- `SemanticEngine` больше не зависит от профиля пользователя и анализирует только входящее сообщение.
- `HumanModelEngine` больше не вызывает `SemanticEngine` самостоятельно, а принимает готовый `SemanticContext`.
- `DialogEngine` изменён на последовательность:
  `DialogEngine → SemanticEngine → HumanModelEngine`.
- В `HumanModel` добавлена структура `understanding_score`.
- Из HumanModel исключены технические поля профиля:
  `history`, `last_question`, `discovery_complete`.

Проверка

- ✅ Проект успешно запускается.
- ✅ Новый пайплайн выполняется без ошибок.
- ✅ Последовательность обработки соответствует спецификации V2.
- ✅ Семантический анализ пока реализован как заглушка (ожидаемое состояние).
- ✅ Поведение программы сохранено.

Git

Коммит:

refactor: align message pipeline with V2 architecture

Hash:

5895b06

Следующий этап

- Реализация полноценного SemanticEngine.
- Заполнение SemanticContext результатами анализа сообщения.
- Начало расчёта Understanding Score.

================================

================================

### Рефакторинг №3 — завершён

Дата: 30.07.2026

Что сделано

- Приведён конвейер обработки сообщений в соответствие с архитектурой V2.
- SemanticEngine теперь анализирует только входящее сообщение пользователя.
- HumanModelEngine больше не вызывает SemanticEngine самостоятельно, а принимает готовый SemanticContext.
- DialogEngine перестроен на последовательность:
  DialogEngine → SemanticEngine → HumanModelEngine.
- В HumanModel добавлена структура understanding_score.
- Из HumanModel исключены технические поля профиля (history, last_question, discovery_complete).

Проверка

✅ Бот успешно запускается.
✅ Новый пайплайн работает без ошибок.
✅ Ответ модели формируется корректно.
✅ Профиль пользователя обновляется корректно.
✅ Архитектура соответствует спецификации V2.

Git

Commit:
5895b06

Message:
refactor: align message pipeline with V2 architecture

Статус

✔ Завершено.
==================================================================

===============================================================================

## Архитектурная фаза завершена

**Дата:** 03.08.2026

**Commit:**

a9156f7

Complete architecture and initialize development infrastructure

### Что сделано

- Завершена архитектурная подготовка проекта.
- Создан полный комплект архитектурной документации (`docs/`).
- Архивированы предыдущие версии архитектуры (`architecture_history/`).
- Подготовлен каркас Engine:
  - Semantic Engine
  - Human Model Engine
  - Reasoning Engine
  - Decision Engine
  - Impact Engine
- Созданы Development Protocol и Implementation Plan.
- Добавлена инфраструктура тестирования (`pytest`).
- Добавлены smoke-тесты проекта.
- Создан `requirements.txt`.
- Настроен `pytest.ini`.
- Обновлён `.gitignore`.
- Проект переведён в состояние, пригодное для поэтапной реализации.

### Что проверено

- Бот успешно запускается.
- DialogEngine работает корректно.
- Smoke-тесты проходят успешно (`2 passed`).
- Импорт проекта выполняется без ошибок.
- Проект успешно закоммичен.
- Изменения успешно отправлены в GitHub.

### Итог

Архитектурная подготовка проекта завершена.

С этого момента документация считается утверждённой и используется как единственный источник истины при разработке.

Дальнейшая реализация ведётся строго по `docs/15_IMPLEMENTATION_PLAN.txt`.

Работа переходит из стадии проектирования в стадию реализации.

Все дальнейшие изменения выполняются по процессу:

**Phase → Проверка → Commit → Push → Следующий Phase.**

### Следующий этап

Начало реализации **Phase 2** согласно Implementation Plan.

===============================================================================

===============================================================================

## Phase 2 завершён — Semantic Engine реализован

**Дата:** 03.08.2026

**Commit:**

b6ce025

Implement Semantic Engine

### Что сделано

- Semantic Engine перестал быть заглушкой и реализован как полноценный первый этап конвейера AI Consultant.
- Реализован анализ одного текущего сообщения без использования LLM.
- Добавлено извлечение явных фактов:
  - возраст;
  - рост;
  - вес;
  - длительность проблемы.
- Реализовано определение:
  - тем сообщения;
  - намерения пользователя;
  - вопросов;
  - явной цели;
  - предпочтений;
  - ограничений;
  - эмоциональных сигналов.
- Добавлена оценка уверенности анализа (`confidence`).
- Исключены ложные определения возраста из конструкций вида «уже 5 лет».
- Сохранена совместимость с текущим Human Model Engine без изменения существующего маршрута работы бота.

### Добавлены тесты

- Создан набор тестовых сообщений для Semantic Engine.
- Добавлены изолированные тесты Semantic Engine.
- Проверены:
  - извлечение явных фактов;
  - определение вопросов;
  - обработка неоднозначных сообщений;
  - обработка пустых сообщений;
  - корректное определение длительности проблемы.

### Что проверено

- Все автоматические тесты успешно пройдены (`7 passed`).
- Smoke-тест приложения проходит успешно.
- DialogEngine продолжает работать без изменений.
- Бот успешно запускается вручную.
- Ответ пользователю формируется корректно.
- Проект успешно закоммичен.
- Изменения успешно отправлены в GitHub.

### Итог

Первый рабочий Engine проекта реализован.

Semantic Engine теперь формирует объективный `SemanticContext`, который станет входными данными для следующих этапов Consultative Loop.

Пользовательское поведение бота при этом осталось неизменным, что подтверждает корректную изоляцию нового Engine и отсутствие регрессий.

### Следующий этап

Начало реализации **Phase 3 — Human Model Engine** согласно `docs/15_IMPLEMENTATION_PLAN.txt`.

===============================================================================

===============================================================================

## Phase 3 завершён — Human Model Engine реализован

**Дата:** 03.08.2026

**Commit:**

1b2dc07

Implement Human Model Engine

### Что сделано

- Реализован полноценный жизненный цикл Human Model.
- Memory разделена на независимые части:
  - подтверждённые факты;
  - историю диалога;
  - техническое состояние консультации;
  - последнюю Human Model.
- Human Model расширена в соответствии с `docs/05.2_HUMAN_MODEL.txt`.
- HumanModelEngine теперь использует:
  - SemanticContext;
  - предыдущую Human Model;
  - Memory пользователя.
- Новая информация из текущего сообщения имеет приоритет над ранее сохранёнными данными.
- Исключено попадание технических полей (`history`, `last_question`, `discovery_complete` и других служебных данных) в Human Model.
- DialogEngine сохраняет новую Human Model после завершения каждого цикла.
- Telegram-обработчик передаёт `user_id`, что обеспечивает сохранение Human Model между сообщениями пользователя.

### Добавлены тесты

- Созданы тесты HumanModelEngine.
- Созданы тесты жизненного цикла Memory.
- Проверены:
  - создание новой Human Model;
  - корректное обновление существующей модели;
  - сохранение Human Model между последовательными циклами;
  - отсутствие технических данных внутри Human Model;
  - корректное обновление только подтверждённых фактов.

### Что проверено

- Все автоматические тесты успешно пройдены (`12 passed`).
- Smoke-тест приложения проходит успешно.
- Бот успешно запускается вручную.
- DialogEngine продолжает работать корректно.
- Human Model корректно сохраняется между сообщениями.
- Проект успешно закоммичен.
- Изменения успешно отправлены в GitHub.

### Итог

Human Model стала самостоятельной архитектурной сущностью проекта.

Теперь система умеет не только анализировать текущее сообщение пользователя, но и сохранять непрерывную модель человека между циклами консультации.

Это создаёт основу для реализации Reasoning Engine, который сможет принимать решения, опираясь не только на текущее сообщение, но и на накопленное понимание пользователя.

### Следующий этап

Начало реализации **Phase 4 — Reasoning Engine** согласно `docs/15_IMPLEMENTATION_PLAN.txt`.

===============================================================================

===============================================================================

## Phase 4 завершён — Reasoning Engine реализован

**Дата:** 03.08.2026

**Commit:**

12e0c56

Implement Reasoning Engine

### Что сделано

- Реализован полноценный Reasoning Engine как отдельный слой анализа между Human Model и Decision Engine.
- ReasoningContext приведён в соответствие с `docs/05.3_REASONING_CONTEXT.txt`.
- Добавлены:
  - выводы;
  - рабочие гипотезы;
  - подтверждённые гипотезы;
  - опровергнутые гипотезы;
  - неопределённости;
  - противоречия;
  - недостающая информация;
  - приоритетные области исследования;
  - основания выводов;
  - уровень уверенности.
- ReasoningEngine теперь получает:
  - SemanticContext;
  - Human Model;
  - Memory пользователя.
- Реализовано построение объяснимых выводов исключительно на основании доступных данных.
- Неопределённость сохраняется явно и не заменяется предположениями.
- Обнаруженные противоречия фиксируются, но не разрешаются на этапе рассуждения.
- Human Model остаётся неизменной во время работы Reasoning Engine.
- DialogEngine передаёт полный набор входных данных в Reasoning Engine.

### Добавлены тесты

- Созданы изолированные тесты Reasoning Engine.
- Проверены:
  - построение рабочих гипотез;
  - наличие оснований выводов;
  - корректная оценка уверенности;
  - сохранение неопределённостей;
  - фиксация противоречий;
  - отсутствие изменений Human Model;
  - совместимость с предыдущими Phase.

### Что проверено

- Все автоматические тесты успешно пройдены (`15 passed`).
- Smoke-тест приложения проходит успешно.
- Бот успешно запускается вручную.
- DialogEngine продолжает работать корректно.
- Reasoning Engine интегрирован без регрессий.
- Проект успешно закоммичен.
- Изменения успешно отправлены в GitHub.

### Итог

Reasoning Engine стал первым компонентом системы, формирующим объяснимое внутреннее рассуждение консультанта.

Система теперь умеет:

- понимать сообщение;
- поддерживать непрерывную модель пользователя;
- строить логические выводы и рабочие гипотезы, не принимая преждевременных решений.

Это создаёт основу для следующего этапа — Decision Engine, который будет принимать решения, опираясь на сформированное рассуждение.

### Следующий этап

Начало реализации **Phase 5 — Decision Engine** согласно `docs/15_IMPLEMENTATION_PLAN.txt`.

===============================================================================

===============================================================================

## Phase 5 завершён — Decision Engine реализован

**Дата:** 03.08.2026

**Commit:**

bff7162

Implement Decision Engine

### Что сделано

- Реализован полноценный Decision Engine как отдельный этап принятия решений после Reasoning Engine.
- DecisionContext приведён в соответствие с `docs/05.4_DECISION_CONTEXT.txt`.
- Добавлены:
  - одно следующее действие;
  - причина выбора;
  - приоритет;
  - необходимость дополнительной информации;
  - готовность к следующему этапу консультации;
  - ожидаемый результат;
  - уровень уверенности.
- DecisionEngine теперь получает:
  - SemanticContext;
  - Human Model;
  - ReasoningContext;
  - Memory пользователя.
- Реализован выбор единственного следующего действия в соответствии с архитектурным приоритетом:
  1. уточнение противоречия;
  2. исследование наиболее важной неизвестности;
  3. проверка рабочей гипотезы;
  4. продолжение консультации.
- Decision Engine не изменяет Human Model, Reasoning Context и не формирует текст ответа.
- DialogEngine передаёт полный набор входных данных в Decision Engine.

### Добавлены тесты

- Созданы изолированные тесты Decision Engine.
- Проверены:
  - приоритет обработки противоречий;
  - выбор одной наиболее значимой неизвестности;
  - переход к проверке рабочей гипотезы;
  - неизменность Human Model;
  - совместимость с предыдущими Phase.

### Что проверено

- Все автоматические тесты успешно пройдены (`18 passed`).
- Smoke-тест приложения проходит успешно.
- Бот успешно запускается вручную.
- DialogEngine продолжает работать корректно.
- Decision Engine интегрирован без регрессий.
- Проект успешно закоммичен.
- Изменения успешно отправлены в GitHub.

### Итог

Decision Engine стал первым компонентом системы, принимающим объяснимое решение на основе результатов анализа.

Теперь когнитивный конвейер умеет:

- понимать сообщение пользователя;
- поддерживать непрерывную Human Model;
- формировать объяснимые выводы;
- выбирать одно наиболее подходящее следующее действие.

Это завершает этап внутреннего анализа и создаёт основу для следующего слоя — оценки влияния выбранного решения.

### Следующий этап

Начало реализации **Phase 6 — Impact Engine** согласно `docs/15_IMPLEMENTATION_PLAN.txt`.

===============================================================================